# M2 平台缺省层 · 执行记录

> 对应任务书：`1-docs/M2开发任务书-平台缺省层.md`  
> 完成日期：2026-08-07

## 结论

M2 已完成。平台缺省层六份 md 全部接入；课程未写覆盖时行为与搬运前一致（公开包零 diff；话术逐字对照测试全绿）。

## 已落地文件

| 文件 | 对应子任务 | 行为是否改变 |
|---|---|---|
| `_platform/defaults.md` | M2-1 | 否 |
| `_platform/language-levels.md` | M2-2 | 否 |
| `_platform/companion.md` | M2-3 | 否（新建「人设侧重」通路；课程不写时身份段不变） |
| `_platform/voice.md` | M2-4 | 否 |
| `_platform/scaffolding.md` | M2-5 | 否（只升不降；降档未引入） |
| `_platform/tool-defaults.md` | M2-5 | 否（只搬显示名与 label；正则匹配器留代码） |

## 决策落点（相对任务书确认时的选择）

1. **defaults 共享链**：`parseLesson` 可选入参 `platformDefaults`；`sync-lessons` 与服务端编译读同一份。
2. **tool-defaults**：partial——10 个显示名 + `text.observation` label；`/实物|识别/` 等匹配器留代码。
3. **脚手架降档**：不做。
4. **voice 键名**：与 `decision.intent` 一一对齐（`intent.分支`）。

## 已知遗留（搬运前即存在，本阶段锁住不改）

- 年级匹配里 `一|二` 是裸字符：`初一/初二/高一/高二` 会先命中小学低年级。见 `language-levels.test.js`。
- 絮絮素材路径仍在 `platform-config.js`（浏览器构建期要用），四个键锁定。

## 验证

- 新增测试：`platform-defaults` / `language-levels` / `companion` / `voice` / `scaffolding` / `tool-defaults`。
- `dialogue-contract` / `anti-loop` / `agent` 等既有对话测试未改断言即通过。
- `npm run build` 后 `lesson-public.js` 无 diff；`dist` 中无保护词泄漏抽检。
